# エージェント向けガイドライン

このファイルはリポジトリ全体に適用します。より深い階層に `AGENTS.md` がある場合は、そちらを優先してください。

## リポジトリの目的

このリポジトリは、macOS と Linux の個人環境を chezmoi で再現するための dotfiles です。シェル、CLI、Git、tmux、Neovimなどの設定に加え、Homebrewパッケージと初期セットアップスクリプトを管理します。

変更は、既存環境を壊さないこと、繰り返し適用できること、OS差分が明確であることを優先してください。

## コミュニケーション

- 実行結果、コミットメッセージ、PR本文は日本語で記述してください。
- 変更理由、影響範囲、検証結果を簡潔に報告してください。
- 確認できない動作は断言せず、未確認の理由と想定されるリスクを明示してください。

## 正本とディレクトリ構成

- chezmoiのsource rootは `.chezmoiroot` により `home/` です。
- ホームディレクトリに展開されたファイルではなく、必ず `home/` 配下のソースを変更してください。
- 主要な依存関係は `Brewfile`、`Brewfile.darwin`、`mise.toml` で管理します。
- 利用者向けの操作は `README.md`、実装仕様と保守手順は `docs/` を正本とします。

## chezmoi固有の変更規則

- chezmoiの命名規則に従い、展開先のドットファイルは `dot_`、テンプレートは `.tmpl` を使用してください。
- OSや端末ごとの差分にはchezmoiのテンプレート変数を使い、条件と対象環境をコメントで説明してください。
- 初回だけ必要な処理は `run_once_`、入力内容の変更時に再実行すべき処理は `run_onchange_` を使用してください。
- `run_once_` の内容を変更しても、適用済み環境では自動的に再実行されない点を考慮してください。
- セットアップスクリプトは原則として冪等にし、対象が既に存在する場合の挙動を明示してください。
- テンプレートやスクリプトの実行順序に依存する場合は、chezmoiのファイル名プレフィックスで順序を表現してください。

## 安全性と互換性

- 秘密鍵、アクセストークン、Cookie、端末固有の秘密情報をコミットしないでください。
- 既存ファイルの削除、認証情報の登録、外部サービスへの書き込みなど、副作用の大きい処理を無条件で追加しないでください。
- SSH鍵の作成やGitHubへの登録など外部状態を変更する処理は、事前条件を確認し、安全にスキップできる実装にしてください。
- シェルスクリプトは非対話実行を前提に、原則として `set -euo pipefail`、明示的なログ、エラー処理を備えてください。
- macOS固有のcask、パス、コマンドはLinuxで評価されないように条件分岐してください。
- 新しい依存ツールを設定から参照する場合は、導入元となるBrewfileまたはmise設定との整合を確認してください。
- 破壊的なGitエイリアスやシェルエイリアスを追加する場合は、安全策と採用理由を設定内にコメントしてください。

## 変更方針

- 依頼された範囲に限定し、無関係な個人設定を同時に整理しないでください。
- 既存のローカル拡張ファイルを読み込む仕組みは、可能な限り後方互換に保ってください。
- 設定値を変更するときは、利用者の操作、展開先、依存関係、既存環境への影響を確認してください。
- 自動導入処理とアプリケーション自身のbootstrap処理が重複する場合は、責務とフォールバックの意図を文書化してください。
- フォーマットだけの変更と動作変更は、レビューしやすいように分離してください。

## 検証

変更箇所に応じて、実行可能な確認を選択してください。

### 共通

- `git diff --check`
- 変更したMarkdownのリンクと、記載したパス・コマンドの実在確認

### シェルスクリプト

- テンプレートを含まないスクリプトは `bash -n <file>`
- 利用可能であれば `shellcheck <file>`
- テンプレートはchezmoiで展開した内容に対して構文確認

### chezmoi

- `chezmoi diff`
- 実環境への適用が安全な場合のみ `chezmoi apply --dry-run --verbose`
- OS分岐を変更した場合は、確認できなかったOS側のリスクを明記

### Lua・Neovim

- 利用可能であれば `stylua --check home/dot_config/nvim`
- 利用可能であれば `selene home/dot_config/nvim`
- Neovimを起動する確認は、プラグイン導入や外部通信が発生し得ることを考慮

### 依存関係

- Brewfile変更時は、利用可能であれば対象ファイルに対して `brew bundle check`
- mise設定変更時は、利用可能であれば `mise config` などで設定の解決結果を確認

検証できない項目がある場合は、未実施理由と残るリスクを作業報告に含めてください。

## ドキュメント同期

動作や構成を変更した場合は、同じ変更内で対応する文書を更新してください。

| 変更内容 | 更新先 |
| --- | --- |
| 利用者の導入・更新・復旧手順 | `README.md`、`docs/installation.md` |
| chezmoi構成、適用順序、OS分岐 | `docs/architecture.md` |
| Shell、Git、tmux、WezTerm、miseなど | `docs/configuration.md` |
| Neovim、LSP、Formatter、Linter | `docs/neovim.md` |
| 開発・検証手順 | `docs/development.md` |

READMEは利用者向けの簡潔なマニュアルに保ち、内部構造や設定一覧を過剰に追加しないでください。

## 完了条件

- 実装と文書の説明が一致している。
- macOS/Linuxおよび既存環境への影響を確認している。
- 秘密情報や端末固有の生成物が差分に含まれていない。
- 変更に対応する検証を実行し、結果または未実施理由を報告している。
- 利用者の操作が変わる場合はREADMEも更新している。
