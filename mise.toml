# Runtime versions managed via mise.
[tools]
node = "lts"
python = "latest"

[tasks.setup-venv]
description = "Set up a Python virtual environment."
run = [
        "python -m venv .venv",
        "echo '[env]\n_.python.venv = \".venv\"' >> mise.toml"
    ]
